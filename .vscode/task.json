{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run uvicorn",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/uvicorn",
      "args": ["app.main:app", "--reload"],
      "problemMatcher": [],
      "group": "build"
    }
  ]
}

